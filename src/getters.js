const area = ['area']
const uiState = ['uiState']
const rooms = ['rooms']
const mobs = ['mobs']
const selectedRoomId = ['rooms', 'selectedRoomId']

export default {
  area,
  uiState,
  mobs,
  rooms,
  selectedRoomId
}
